# URL бэкенда (при сборке). Для деплоя за одним доменом оставить пустым.
# VITE_API_URL=

# Локальная разработка (прокси в vite.config):
# не задавать или пусто — запросы идут на localhost:8000 через proxy
